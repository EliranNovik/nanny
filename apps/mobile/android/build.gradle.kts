// Root-level (project-level) Gradle file for Tebnu Android (Firebase).
// Merge into your React Native android/build.gradle.kts when the native app is created.

plugins {
    // Add the dependency for the Google services Gradle plugin
    id("com.google.gms.google-services") version "4.5.0" apply false
}
